<nav class="navbar navbar-default">
    <div class="container-fluid">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse"
                    data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            {# lien principale, le titre du site qui permet un retour à la page de présentation #}
            <a class="navbar-brand" href="{{ path('homepage') }}">Mes contacts</a>
        </div>

        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
            <ul class="nav navbar-nav">
                {% if is_granted('ROLE_USER') %}  {# selon si on est "connecté" ou pas, on affiche un message personnalisé avec le nom de l' user #}
                    <li class="navbar-text">Hello {{ app.user.username }}</li>
                    <li><a href="{{ path('contact', {'id': app.user.id}) }}">Carnet d'adresses</a></li>
                    <li><a href="{{ path('insect_logout') }}">Déconnexion</a></li>
                {% else %}   {# sinon on affiche les liens pour l'inscription ou l'authentification #}
                    <li><a href="/register">Inscription des Insectoïdes</a></li>
                    <li><a href="/login">Connexion des Insectes</a></li>
                {% endif %}
            </ul>
        </div><!-- /.navbar-collapse -->
    </div><!-- /.container-fluid -->
</nav>